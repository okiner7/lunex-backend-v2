module.exports = {
  apps: [
    {
      name: 'lunex-api',
      script: 'server.js',

      // Cluster mode — ???? ?????? ?? ?????? ???? CPU
      instances: 'max',
      exec_mode: 'cluster',

      // ?????????????? ???? ?????? > 500MB
      max_memory_restart: '500M',

      // ?????????? ?????????
      env: {
        NODE_ENV: 'production',
        PORT: 3000
      },
      env_development: {
        NODE_ENV: 'development',
        PORT: 3000
      },

      // ????
      out_file: './logs/out.log',
      error_file: './logs/error.log',
      merge_logs: true,
      log_date_format: 'YYYY-MM-DD HH:mm:ss',

      // ??????? ?????????? ??? ?????????
      wait_ready: true,
      listen_timeout: 10000,
      kill_timeout: 5000,

      // ?????????? ??? ?????? ???????
      autorestart: true,
      restart_delay: 1000,
      max_restarts: 10,
      min_uptime: '10s',

      // ?????????? ????????? node_modules ? ?????
      watch: false,
      ignore_watch: ['node_modules', 'logs', 'data', '*.db']
    }
  ]
}
